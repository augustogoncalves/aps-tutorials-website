Inicie o aplicativo do Visual Studio Code (por exemplo, por meio do _Run > Debugging_ Iniciar
ou pressionando 'F5') e navegue até [http://localhost:8080](http://localhost:8080)
no navegador. O servidor deve responder com 'Não é possível obter /' porque não adicionamos nenhum
lógica para ele ainda. Esse será o tema do próximo passo.