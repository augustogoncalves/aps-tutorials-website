Inicie la aplicación desde Visual Studio Code (por ejemplo, a través de la _Run > Inicio Debugging_
o pulsando 'F5') y vaya a [http://localhost:8080](http://localhost:8080)
en el navegador. El servidor debería responder con un error 404 porque no hemos agregado ninguno
lógica a ello todavía. Ese va a ser el tema del siguiente paso.