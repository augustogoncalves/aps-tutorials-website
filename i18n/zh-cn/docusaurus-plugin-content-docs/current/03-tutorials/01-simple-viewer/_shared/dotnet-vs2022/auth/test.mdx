如果应用程序仍在运行，请重新启动它（例如，使用 _Debug > Restart_、
或按“Ctrl”+“Shift”+“F5”），否则再次启动它（使用_Debug >“开始”Debugging_，
或按“F5”）。导航到 [http://localhost:8080/api/auth/token](http://localhost:8080/api/auth/token） 时
在浏览器中，服务器现在应使用包含访问令牌数据的 JSON 对象进行响应。