アプリケーションがまだ実行中の場合は、再起動します (たとえば、_Run > Restart Debugging_ を使用します。
または緑色の再起動アイコンをクリックする)、それ以外の場合は再起動します([スタート]Debugging_を使用して_Run >
または「F5」を押す)。[http://localhost:8080/api/auth/token](http://localhost:8080/api/auth/token)に移動すると
ブラウザーでは、サーバーはアクセストークンデータを含むJSONオブジェクトで応答する必要があります。