Visual Studio Code からアプリケーションを起動します (たとえば、_Run > [スタート] Debugging_
メニューをクリックするか、「F5」を押して)に移動し、[http://localhost:8080](http://localhost:8080)に移動します。
ブラウザで。404 応答が返されるのは、サーバー ロジックがまだ実装されていないためです。
それが次のステップの目標になります。