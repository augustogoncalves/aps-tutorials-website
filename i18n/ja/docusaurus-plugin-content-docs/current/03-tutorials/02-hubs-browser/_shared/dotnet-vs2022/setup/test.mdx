Visual Studio からアプリケーションを起動します (たとえば、_Debug > の [スタート] Debugging_
メニュー、または「F5」を押すと、自動的に[http://localhost:8080](http://localhost:8080)が開きます
をブラウザで。404 応答が返されるのは、サーバー ロジックがまだ実装されていないためです。
それが次のステップの目標になります。